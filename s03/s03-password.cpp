#include <iostream>
#include <string>

auto main(int argc, char* argv[]) -> int
{
std::string haslo;
a = argv[1];
if(haslo==a)
{
    std::cout<<"OK!";
}
    do{
    std::cout << "Podaj haslo: ";
    std::cin>>haslo;
    }while(haslo!=a);
    return 0;
}

