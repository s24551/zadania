#include <iostream>
#include<time.h>

int search(int tab[], int n, int needle)
{
    int licznik=tab[0];
    for(int i=0;i<n;i++)
    {
       if(tab[i]==needle)
       {
           return i;
       }
    }
    return -1;
}
int main()
{
    int rozm,indeks;
    int tab[]={42, 9 , -1, 18, 59, 3, 101, 31, 72, 12};
    std::cout<<"Podaj rozmiar tablicy: ";
    std::cin>>rozm;
    std::cout<<"Podaj liczbe z tablicy: ";
    std::cin>>indeks;
    std::cout<<search(tab, rozm, indeks);
    return 0;
}
